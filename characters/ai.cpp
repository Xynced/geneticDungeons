#include "ai.h"

AI::AI(){}

void AI::action(Controller *controller, QList<Character *> friends,
                QList<Character *> targets)
{
    Q_UNUSED(controller);
    Q_UNUSED(friends);
    Q_UNUSED(targets);
}
